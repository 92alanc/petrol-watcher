language: android
android:
  components:
    - tools
    - platform-tools

  before_script:
    - echo no | android create avd --force -n test android-26 --abi armeabi-v7a
    - emulator -avd test -no-audio -no-window &
    - android-wait-for-emulator
    - adb shell input keyevent 82 &
