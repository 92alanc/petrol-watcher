<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.braincorp.petrolwatcher"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".feature.users.activities.LoginActivity"
            android:finishOnTaskLaunch="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".feature.users.activities.ProfileActivity" />

        <activity android:name=".feature.vehicles.activities.VehiclesActivity"
            android:parentActivityName=".feature.users.activities.ProfileActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".feature.users.activities.ProfileActivity" />
        </activity>

        <activity android:name=".feature.map.activities.MapActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:finishOnTaskLaunch="true" />

        <activity android:name=".feature.petrolstations.activities.PetrolStationsActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".feature.map.activities.MapActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".feature.map.activities.MapActivity" />
        </activity>

        <activity android:name=".feature.petrolstations.activities.FuelsActivity"/>

        <activity android:name=".feature.settings.activities.SettingsActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/settings"
            android:parentActivityName=".feature.map.activities.MapActivity"
            android:finishOnTaskLaunch="true">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".feature.map.activities.MapActivity" />
        </activity>

        <service android:name=".services.StationLocatorService" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>